<form action="/file/rename/<%= file.id%>" method="POST">

<div>
    <label>Origin name: <%= file.filename %></label>
</div>

<div class="form-group">
    <label>New name:</label>
    <input type="filename" class="form-control" name="filename">
</div>

<button type="submit" class="btn btn-primary">Rename</button>
</form>


<!-- <script>
    async function submitForm(oFormElement,id) {

        var response = await fetch("/file/rename"+id, {
            method: oFormElement.method,
            credentials: 'same-origin',
            body: new FormData(oFormElement),
        });

        if (response.ok) {
            // for res.ok() or res.json()
            alert("rename suceesful");
            location.href = "/file/view"
            
        } else if (response.status == 401) {
            var data = await response.text();   // for res.send()
            alert(data);
        } else {
            alert(response.statusText);
        }


    };

</script> -->